"use server";

// Help actions could fetch articles, submit a support ticket, or connect to a knowledge base.
export async function getHelpArticles() {
  // Replace with help docs backend call
  return [];
}