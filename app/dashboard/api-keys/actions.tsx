"use server";

// For production, handle CRUD of API keys in here (e.g. create, revoke, list)
export async function getApiKeys(userId: string) {
  // Replace with DB lookup of user's API keys
  return [];
}